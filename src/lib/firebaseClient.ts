// Deprecated Firebase client removed after switching to Supabase.
// Retained as stub to avoid stale import errors during incremental build.
export function initFirebase(){ return null; }
export function getCurrentUserAsync(){ return Promise.resolve(null); }
